if(Find labs without energy) {
	fill creep with energy
	fill labs without energy
} else if(Find labs without resources) {
	which lab needs what?
	find what
	fill lab
} else {
   //container with energy 
  // product -> storage .. 50%?
}
